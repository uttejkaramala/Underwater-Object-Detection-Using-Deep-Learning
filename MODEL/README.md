# YOLOv11 Model Weights

This directory is intended to store the trained YOLOv11 model.

Expected structure:

```text
model/
└── best.pt
```

The `best.pt` file is not included in this repository because of GitHub file size limitations.

Place your trained YOLOv11 weights in this folder before running the application.